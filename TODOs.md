# TODOs

- Add smoke tests for `run`, `run-tvm`, and `verify` on a tiny sample set.
- Add explicit decoder attention mask support for T5 decoding.
- Add an artifact manifest + loader validation (checksum, shape bounds, target metadata).
- Add a small performance regression script that runs `profile` and stores CSV in `python/dist`.
